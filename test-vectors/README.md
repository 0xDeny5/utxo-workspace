# Test vectors

Language-neutral golden cases for `utxo-coinselect`.

| File          | Role                                                 |
| ------------- | ---------------------------------------------------- |
| `core.json`   | Weight + selection fixtures (normative expectations) |
| `schema.json` | Loose JSON Schema for `core.json`                    |

## Spec

Behavioral rules live in `[spec/coin-selection.md](../spec/coin-selection.md)` (including the PRNG contract for seeded strategies).
