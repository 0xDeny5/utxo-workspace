# utxo-descriptors

## 0.1.0

### Minor Changes

- 2274516: Initial release: a zero-dependency BIP-380 output descriptor parser, covering the script expressions of BIP-381 through BIP-386 and BIP-389 multipath, with `inputWeight()`/`outputWeight()` and `describeInput()`/`describeOutput()` that agree with `utxo-coinselect`'s weight catalog.
